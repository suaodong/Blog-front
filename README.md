# Blog Frontend (ANTIGRAVITY)

这是一个基于 **Vue 3** + **TypeScript** + **Vite** 构建的现代化博客前端项目。采用 **Tailwind CSS** 进行响应式设计，注重用户体验与视觉交互。

## ✨ 特性

- **🎨 现代化 UI 设计**
  - 响应式布局，适配移动端与桌面端。
  - 玻璃拟态 (Glassmorphism) 头部导航栏。
  - 粒子背景特效 (Particles.js)。
  - 平滑的过渡动画与交互反馈。

- **🌓 主题切换**
  - **暗色模式 (Dark Mode)**: 沉浸式阅读体验，深色背景搭配亮色文字。
  - **护眼模式 (Light Mode)**: 专为长时间阅读设计，采用 **淡青绿 (#C7EDCC)** 作为主背景色，有效缓解视觉疲劳。

- **🧭 导航与搜索**
  - **居中布局**: 顶部导航栏采用经典的 "Logo(左) - 菜单(中) - 工具(右)" 布局。
  - **动态搜索框**: 搜索图标悬停/点击时平滑展开输入框，保持界面简洁。

- **📄 文章阅读**
  - **Markdown 渲染**: 支持富文本 Markdown 格式渲染。
  - **沉浸式详情页**: 大图封面，优化的排版。
  - **返回顶部**: 页面滚动后显示 "纸飞机" 悬浮按钮，一键平滑回到顶部。

- **⚡️ 技术栈**
  - **核心框架**: [Vue 3](https://vuejs.org/) (Composition API)
  - **构建工具**: [Vite](https://vitejs.dev/)
  - **语言**: [TypeScript](https://www.typescriptlang.org/)
  - **样式库**: [Tailwind CSS](https://tailwindcss.com/)
  - **状态管理**: [Pinia](https://pinia.vuejs.org/)
  - **路由**: [Vue Router](https://router.vuejs.org/)
  - **HTTP 请求**: [Axios](https://axios-http.com/)
  - **特效**: [tsparticles](https://particles.js.org/)

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm 或 yarn / pnpm

### 安装依赖

```bash
npm install
# 或者
yarn install
# 或者
pnpm install
```

### 启动开发服务器

```bash
npm run dev
```
项目将在 `http://localhost:5173` (默认端口) 启动。

### 构建生产版本

```bash
npm run build
```

## 📂 项目结构

```
src/
├── api/            # API 接口封装
├── assets/         # 静态资源 (图片, SVG 等)
├── components/     # 公共组件 (Header, Footer 等)
├── router/         # 路由配置
├── stores/         # Pinia 状态管理
├── types/          # TypeScript 类型定义
├── utils/          # 工具函数 (如 request.ts)
├── views/          # 页面视图 (ArticleList, ArticleDetail 等)
├── App.vue         # 根组件
└── main.ts         # 入口文件
```

## 🔧 配置说明

### 接口配置
在 `src/utils/request.ts` 中配置后端 API 的 `baseURL`。

### 路由配置
在 `src/router/index.ts` 中管理页面路由。
